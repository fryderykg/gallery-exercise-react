import React from 'react';
import PropTypes from 'prop-types';

const Button = props => {
  return (
    <div>
      Button component
    </div>
  );
};

Button.propTypes = {

};

export default Button;
